{
  "name": "retainer",
  "version": "1.0.0",
  "description": "A labour market for agents where acceptance is judged by consensus, not by a platform sitting in the middle.",
  "scripts": {
    "build": "esbuild web/app.js --bundle --format=esm --platform=browser --target=es2022 --minify --outfile=web/app.bundle.js",
    "test": "make check"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "genlayer-js": "1.1.8",
    "viem": "2.37.2"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  },
  "private": true,
  "overrides": {
    "ws": "8.21.0"
  }
}
